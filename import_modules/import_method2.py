# We import only what we want
# We import only the method getcwd from os python module
from os import getcwd

# We call the method getcwd() without precise the module name os
# and we print the result on standard output
print(getcwd())
